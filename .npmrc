# Ensure all optional dependencies are installed
optional=true
# Force npm to install all platform-specific binaries
target_platform=linux
target_arch=x64
# Disable package-lock for cleaner installs
package-lock=false
